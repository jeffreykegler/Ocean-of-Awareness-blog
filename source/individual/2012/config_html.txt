A Configurable HTML Parser
    <p>
      <!--
      perl ./hide/marpa_r2_html_fmt
      -->
      This post introduces an HTML parser which is both liberal and configurable.
      I have already explained some of the ideas behind it -- others are new.
      Let me start with a simple example.
<h1>Why configurable?</h1>
First off, why do we want a configurable HTML parser?
The new, configurable
